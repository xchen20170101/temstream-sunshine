# Sunshine Server Configuration
# Server IP address for management system communication
server_ip = 127.0.0.1

# Server port for management system communication
server_port = 12345

# Enable Tailscale IP usage
isTailscale = true

# Tailscale auto-login configuration
tailscale_authkey =
headscale_url = https://hsmanage.ccwu.cc

# LAN Mode Settings
# Enable to allow clients on the same network to connect with fixed PIN
lan_fixed_pin = false

# Password for LAN mode authentication (required when LAN mode is enabled)
lan_password =

# Current logged in user
login_user =
